{
  "name": "@controlkit",
  "version": "0.1.0",
  "packageManager": "bun@1.1.18",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "turbo run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "gh-pages": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "turbo": "^2.0.6"
  }
}
